export function Projects() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Projects</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        A grid of projects will go here. Wire this up to the `projects` table.
      </p>
    </section>
  );
}
