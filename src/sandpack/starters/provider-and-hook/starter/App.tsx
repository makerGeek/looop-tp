// TODO: build AuthContext.ts, AuthProvider.tsx, useAuth.ts in separate files.
// Then wrap <Greeting /> and <LoginPanel /> in <AuthProvider>.

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>Wire up the provider/hook pattern across multiple files.</p>
    </div>
  );
}
