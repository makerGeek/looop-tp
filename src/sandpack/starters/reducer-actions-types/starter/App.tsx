// TODO: split state/actions/reducer into 3 files under ./cart/, then consume
// via useReducer in CartView.tsx.

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>Refactor the cart into a typed reducer + actions + types triad.</p>
    </div>
  );
}
