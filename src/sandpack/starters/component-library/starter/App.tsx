// TODO: build a `lib/ui/` micro-library with tokens, cn helper, Button and
// Card primitives, then consume `import { Button, Card } from "./lib/ui"`.

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>Compose with primitives from your tiny component library.</p>
    </div>
  );
}
