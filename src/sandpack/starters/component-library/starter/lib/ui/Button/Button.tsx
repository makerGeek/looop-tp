// TODO: a Button primitive with variant="primary" | "ghost".
// Consume styles from `../tokens`.

import type { ButtonHTMLAttributes } from "react";

export function Button(_props: ButtonHTMLAttributes<HTMLButtonElement>) {
  return <button />;
}
