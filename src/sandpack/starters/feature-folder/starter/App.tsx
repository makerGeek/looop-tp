// TODO: build a `features/todos/` folder with TodoList, TodoForm, useTodos,
// types.ts and an index.ts barrel. Then import from "./features/todos".

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>Compose &lt;TodoForm /&gt; and &lt;TodoList /&gt; from a feature folder.</p>
    </div>
  );
}
