// Read-only walkthrough — see the solution view for the full file tree.
export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>Read-only exercise — see the solution tab.</p>
    </div>
  );
}
