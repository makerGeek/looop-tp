// TODO: build a Card component with { title, children, footer? } props.

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui", display: "grid", gap: 16 }}>
      {/* TODO: use <Card title="Welcome"> with some children */}
      <p>Cards will go here.</p>
    </div>
  );
}
