// TODO: Container — owns data/state/effects.
// - useState for users + selectedId + loading
// - useEffect to fetchUsers() (fake 400ms delay)
// - render <UsersList> with the wired handlers
// No JSX layout details here except the skeleton while loading.

export function UsersContainer() {
  return null;
}
