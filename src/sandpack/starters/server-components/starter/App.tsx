// Read-only: this exercise demonstrates Server Components in Next.js 15.
// The sandbox iframe cannot run Node, so we show the authoritative code here.
export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>See the solution tab for the full server/client split.</p>
    </div>
  );
}
